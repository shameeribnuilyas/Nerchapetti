import React from 'react';
import classes from '../css/logo.module.css';

const Logo = () => {
    return(
        <div className={classes.logo}>
            <h1>LOGO</h1>
        </div>
    );
}

export default Logo;